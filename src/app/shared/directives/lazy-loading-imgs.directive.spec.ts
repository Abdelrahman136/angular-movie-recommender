import { LazyLoadingImgsDirective } from './lazy-loading-imgs.directive';

describe('LazyLoadingImgsDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadingImgsDirective();
    expect(directive).toBeTruthy();
  });
});
